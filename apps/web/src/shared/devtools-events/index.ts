/**
 * Window CustomEvent names for keyboard shortcuts ↔ demo/telemetry panels.
 * Lives in shared so those features never import each other (FSD).
 */
export const TOGGLE_DEMO_EVENT = "soulside:toggle-demo";
export const TOGGLE_TELEMETRY_EVENT = "soulside:toggle-telemetry";
